import * as React from 'react';
import * as ReactDOM from 'react-dom';

const App: React.FC = () => {
  return (
    <div>
      <h1>Hello, React TypeScript Node!</h1>
    </div>
  );
};

ReactDOM.render(<App />, document.getElementById('root'));

